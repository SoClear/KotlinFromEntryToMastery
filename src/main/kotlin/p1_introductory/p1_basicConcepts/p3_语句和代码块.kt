package p1_introductory.p1_basicConcepts

/*
在java中，每条语句后都需要用分号（;）作为终止符。
而kotlin没有此要求，只有在同一行编写多条语句时才必须在语句后加上分号。
在一行中编写多条语句是不推荐的。

在kotlin中，一条语句并不代表只有一行，语句是可以跨行编写的。
但是，只有在当前行的行尾是操作符，或者下一行的行首是操作符时，换行才是合法的。

代码块是用大括号括起来的一组语句。
一个代码块代表一个封闭的作用域。
代码块外的成员通常无法访问代码块内的成员，但是反过来是可以的。
 */